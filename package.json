{
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc -w & npx wrangler pages dev ./site",
    "deploy": "npx tsc && npx wrangler pages deploy ./site"
  },
  "dependencies": {
    "jspdf": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.24.3"
  }
}
